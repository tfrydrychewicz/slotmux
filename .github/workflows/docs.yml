name: Docs

on:
  push:
    branches: [main]

jobs:
  deploy-docs:
    name: Deploy documentation
    runs-on: ubuntu-latest
    # Placeholder: enable when docs site exists (e.g. VitePress in docs/)
    if: false
    steps:
      - uses: actions/checkout@v4
      - run: echo "Docs deployment will be configured when docs site is added"
